import io

function fib(int a) -> int
{
    return fib(a - 1) + fib(a - 2)
}

function fib(int a) -> function
{
    return 10
}

function main()
{
    int b := fib()
    function temp() -> int
    {
        return 10
    }

    for(int i := 0; i < 10; i += 1)
    {
        io.print(i)
    }
    while(a < 10 + 1 + 1)
    {
    }
}
